{
  "nodeModulesDir": "auto",
  "workspace": [
    "packages/denote",
    "packages/denote-init",
    "docs"
  ],
  "exclude": [
    ".worktrees"
  ],
  "tasks": {
    "build": "cd docs && deno task build",
    "dev": "cd docs && deno task dev",
    "start": "cd docs && deno task start",
    "validate": "cd docs && deno task validate",
    "test": "rm -rf .coverage && deno test -A --coverage=.coverage && deno coverage .coverage --lcov > lcov.info",
    "check": "deno fmt --check && deno lint && deno check && deno task test && deno task validate",
    "ok": "deno task check && deno task build"
  }
}
