services:
  denote:
    build: .
    ports:
      - "8000:8000"
    volumes:
      # Mount your content directory for live updates
      - ./content:/app/content:ro
      - ./denote.config.ts:/app/denote.config.ts:ro
    restart: unless-stopped
